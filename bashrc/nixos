source `which git | sed 's|\(.*\)/.*|\1|'`/../share/git/contrib/completion/git-prompt.sh

alias nix-list-user-pkgs="nix-env -q '*'"
alias nix-update-user-pkgs="nix-env -u '*'"

export CDPATH_POST=$CDPATH_POST:~/nixsrc

export NIXCFG_REPO=$HOME/src
export NIXCFG_BRANCHES=jira:release-14.04
export NIX_BUILD_CORES=8
